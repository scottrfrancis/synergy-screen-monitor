# Synergy Screen Monitor Configuration Template
# Copy this file to .env and customize for your setup

# === Deployment Role ===
# primary = runs waldo service (log monitor) + optional found-him (alerts)
# secondary = runs found-him only (alerts when specific desktop is active)
ROLE=primary

# === Synergy Configuration ===
# Path to Synergy log file (required for primary machines)
# macOS default: ~/Library/Logs/Synergy/synergy.log
# Linux default: ~/.local/share/synergy/synergy.log
# Windows default: %LOCALAPPDATA%\Synergy\synergy.log
SYNERGY_LOG_PATH=/Users/username/Library/Logs/Synergy/synergy.log

# Target desktop for local alerts (optional for primary, required for secondary)
# Set to the desktop/computer name you want to be alerted about
TARGET_DESKTOP=

# === MQTT Broker Configuration ===
# MQTT broker hostname or IP address
MQTT_BROKER=localhost

# MQTT broker port (default: 1883)
MQTT_PORT=1883

# MQTT topic for desktop switching events
MQTT_TOPIC=synergy

# MQTT client type (nanomq for high performance, paho for compatibility)
# Note: nanomq requires building with ./build.sh
MQTT_CLIENT_TYPE=paho

# === Logging Configuration ===
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=ERROR

# Enable debug mode (true/false)
# When true, enables verbose logging to console
DEBUG_MODE=false

# Directory for application logs
LOG_DIR=./logs

# === Watchdog Configuration (Optional) ===
# How often to check service health (seconds)
WATCHDOG_CHECK_INTERVAL=30

# Maximum restart attempts within the restart window
WATCHDOG_MAX_RESTARTS=3

# Restart window duration (seconds) - default 5 minutes
WATCHDOG_RESTART_WINDOW=300

# Broker connectivity check timeout (seconds)
WATCHDOG_BROKER_TIMEOUT=5
